import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wealth.compliance',
  appName: 'Compliance Monitor',
  webDir: 'dist'
};

export default config;
