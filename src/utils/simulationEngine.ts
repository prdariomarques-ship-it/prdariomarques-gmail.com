export type SimulationScenario = any;
export type ProjectionScenario = any;
export const resolvePortfoliosAndAlertsForMode = (portfolios: any, alerts: any, mode: any, scene1: any, scene2: any) => ({ portfolios, alerts });
